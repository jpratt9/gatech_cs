public class Collections {
    public static void main(String[] args) {
        // List -- interface
            // ArrayList
            // ordered
        //List a = new List(); // cannot do this 
        List a = new ArrayList(); // can do this
        // Set -- interface
            // Hashset
            // unordered - it sorts itself, not the order you give it
            // no duplicates
            // much more efficient than List with time (works in O(1))
            //      because it can't have duplicates and it orders 
            //      itself
        // Map -- interface
            // HashMap
            // unordered
            // key, value pairs
            // keys are stored in a set
    }
}